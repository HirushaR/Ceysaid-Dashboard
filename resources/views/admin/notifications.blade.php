<x-layouts.admin title="Notifications"><div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Workspace</p><h1>Notifications</h1><p>Assignment and workflow alerts.</p></div></div><section class="panel divide-y divide-slate-100 dark:divide-slate-800">@forelse(auth()->user()->notifications()->latest()->paginate(30) as $notification)<article class="p-5 {{ $notification->read_at?'':'bg-blue-50/40 dark:bg-blue-500/5' }}"><p class="font-semibold">{{ data_get($notification->data,'title','Notification') }}</p><p class="mt-1 text-sm text-slate-600 dark:text-slate-300">{{ data_get($notification->data,'body',data_get($notification->data,'message')) }}</p><p class="mt-2 text-xs text-slate-400">{{ $notification->created_at->diffForHumans() }}</p></article>@empty<p class="empty-state">No notifications.</p>@endforelse</section></div></x-layouts.admin>
