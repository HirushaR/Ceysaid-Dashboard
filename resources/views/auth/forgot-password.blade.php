<!DOCTYPE html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Reset password · TravelSync</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="grid min-h-screen place-items-center bg-slate-950 p-5"><main class="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl"><h1 class="text-2xl font-bold">Reset your password</h1><p class="mt-2 text-sm text-slate-500">We’ll email you a secure reset link.</p>@if(session('status'))<p class="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-700">{{ session('status') }}</p>@endif<form method="POST" action="{{ route('admin.password.email') }}" class="mt-6 space-y-5">@csrf<div><label class="form-label" for="email">Email</label><input class="form-input" id="email" name="email" type="email" value="{{ old('email') }}" required autofocus>@error('email')<p class="form-error">{{ $message }}</p>@enderror</div><button class="btn-primary w-full justify-center">Send reset link</button></form><a href="{{ route('admin.login') }}" class="mt-5 block text-center text-sm text-blue-600">Back to sign in</a></main></body></html>
