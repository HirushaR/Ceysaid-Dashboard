<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Analytics</p><h1>Business performance</h1><p>Pipeline, revenue, collections and gross profit.</p></div><div class="flex gap-2"><a href="{{ route('admin.analytics.staff') }}" class="btn-secondary">Staff performance</a><a href="{{ route('admin.exports.download',['type'=>'leads','from'=>$from,'to'=>$to]) }}" class="btn-primary">Export leads</a></div></div><section class="panel p-4"><div class="grid gap-3 md:grid-cols-3"><input wire:model.live="from" type="date" class="form-input"><input wire:model.live="to" type="date" class="form-input">@if(auth()->user()->isAdmin()||auth()->user()->isAccount())<select wire:model.live="sales_user" class="form-input"><option value="">All sales users</option>@foreach($salesUsers as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select>@endif</div></section><section class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">@foreach([['Leads',$summary['leads']],['Converted',$summary['converted']],['Conversion',$summary['conversion_rate'].'%'],['Revenue','LKR '.number_format($summary['revenue'],2)],['Received','LKR '.number_format($summary['received'],2)],['Receivable','LKR '.number_format($summary['receivable'],2)],['Supplier costs','LKR '.number_format($summary['costs'],2)],['Gross profit','LKR '.number_format($summary['profit'],2)]] as [$label,$value])<article class="metric-card"><p>{{ $label }}</p><strong>{{ $value }}</strong></article>@endforeach</section><section class="panel"><div class="panel-header"><h2>Pipeline breakdown</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Stage</th><th>Leads</th><th>Share</th></tr></thead><tbody>@foreach($pipeline as $row)<tr><td>{{ \App\Enums\LeadStatus::tryFrom($row->status)?->label() ?? $row->status }}</td><td>{{ $row->total }}</td><td><div class="h-2 max-w-md rounded bg-slate-100 dark:bg-slate-800"><div class="h-2 rounded bg-blue-600" style="width:{{ $summary['leads']?round($row->total/$summary['leads']*100):0 }}%"></div></div></td></tr>@endforeach</tbody></table></div></section></div>
