<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Call centre</p><h1>Assigned calls</h1><p>Pre-departure and post-arrival customer follow-ups.</p></div></div><section class="panel"><div class="grid gap-3 p-4 md:grid-cols-3"><input wire:model.live.debounce.300ms="search" class="form-input" placeholder="Search customer"><select wire:model.live="type" class="form-input"><option value="">All call types</option>@foreach($types as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select><select wire:model.live="status" class="form-input"><option value="">All statuses</option>@foreach($statuses as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Customer</th><th>Type</th><th>Status</th><th>Travel date</th><th>Assigned</th><th>Attempts</th></tr></thead><tbody>@forelse($calls as $call)<tr><td><a href="{{ route('admin.calls.show',$call) }}" class="font-semibold text-blue-600">{{ $call->lead?->customer_name }}</a><p class="text-xs text-slate-500">{{ $call->lead?->reference_id }}</p></td><td>{{ $call->getCallTypeLabel() }}</td><td><span class="status-badge">{{ $call->getStatusLabel() }}</span></td><td>{{ ($call->call_type==='pre_departure'?$call->lead?->depature_date:$call->lead?->arrival_date)?->format('d M Y') }}</td><td>{{ $call->assignedUser?->name ?: '—' }}</td><td>{{ $call->call_attempts }}</td></tr>@empty<tr><td colspan="6" class="empty-state">No calls match these filters.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $calls->links() }}</div></section></div>
