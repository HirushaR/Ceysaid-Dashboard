<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">HR management</p><h1>Office closures</h1><p>Public holidays and planned office closures.</p></div></div><div class="grid gap-6 lg:grid-cols-[.7fr_1.3fr]"><form wire:submit="save" class="panel space-y-4 p-6"><h2 class="font-bold">Add closure</h2><div><label class="form-label">Title</label><input wire:model="title" class="form-input"></div><div><label class="form-label">Type</label><select wire:model="type" class="form-input">@foreach($types as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></div><div class="grid grid-cols-2 gap-3"><input wire:model="start_date" type="date" class="form-input"><input wire:model="end_date" type="date" class="form-input"></div><textarea wire:model="description" class="form-input" placeholder="Description"></textarea><button class="btn-primary">Add closure</button></form><section class="panel"><div class="panel-header"><h2>Closure schedule</h2></div><div class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($closures as $closure)<div class="flex items-center justify-between p-4"><div><p class="font-semibold">{{ $closure->title }}</p><p class="text-sm text-slate-500">{{ $closure->type->getLabel() }} · {{ $closure->start_date->format('d M Y') }} – {{ $closure->end_date->format('d M Y') }}</p></div><button wire:click="delete({{ $closure->id }})" wire:confirm="Delete this closure?" class="text-sm font-semibold text-rose-600">Delete</button></div>@empty<div class="empty-state">No closures configured.</div>@endforelse</div></section></div></div>
