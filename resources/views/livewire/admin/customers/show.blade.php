<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Operations / Customers</p><h1>{{ $customer->name }}</h1><p>{{ data_get($customer->contact_info,'email') ?: 'No email' }} · {{ data_get($customer->contact_info,'phone') ?: 'No phone' }}</p></div><a href="{{ route('admin.customers.edit',$customer) }}" class="btn-primary">Edit customer</a></div><section class="grid gap-4 md:grid-cols-3">@foreach([['Invoiced',$summary['invoiced']],['Received',$summary['received']],['Outstanding',$summary['outstanding']]] as [$label,$value])<article class="metric-card"><p>{{ $label }}</p><strong>LKR {{ number_format($value,2) }}</strong></article>@endforeach</section><section class="panel"><div class="panel-header"><h2>Travel and finance history</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Lead</th><th>Destination</th><th>Status</th><th>Quotes</th><th>Invoices</th><th>Outstanding</th></tr></thead><tbody>@forelse($customer->leads as $lead)<tr><td><a href="{{ route('admin.leads.show',$lead) }}" class="font-semibold text-blue-600">{{ $lead->reference_id ?: '#'.$lead->id }}</a></td><td>{{ $lead->destination ?: '—' }}</td><td><span class="status-badge">{{ \App\Enums\LeadStatus::tryFrom((string)$lead->status)?->label() ?? $lead->status }}</span></td><td>{{ $lead->quotes->count() }}</td><td>{{ $lead->invoices->count() }}</td><td>LKR {{ number_format($lead->invoices->sum('balance_amount'),2) }}</td></tr>@empty<tr><td colspan="6" class="empty-state">No linked leads.</td></tr>@endforelse</tbody></table></div></section></div>
