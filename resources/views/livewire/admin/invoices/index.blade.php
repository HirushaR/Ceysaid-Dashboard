<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance</p><h1>Invoices</h1><p>Customer balances derived from recorded receipts.</p></div></div><section class="panel"><div class="p-4"><input wire:model.live.debounce.300ms="search" class="form-input max-w-lg" placeholder="Search invoice or customer…"></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Invoice</th><th>Customer</th><th>Date</th><th>Status</th><th>Total</th><th>Balance</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td><a class="table-link" href="{{ route('admin.invoices.show',$invoice) }}">{{ $invoice->invoice_number }}</a></td><td>{{ $invoice->lead?->customer_name }}</td><td>{{ $invoice->invoice_date?->format('d M Y') }}</td><td><span class="status-badge">{{ ucfirst($invoice->customer_payment_status) }}</span></td><td>LKR {{ number_format((float)$invoice->total_amount,2) }}</td><td class="font-semibold">LKR {{ number_format((float)$invoice->balance_amount,2) }}</td></tr>@empty<tr><td colspan="6" class="empty-state">No invoices found.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $invoices->links() }}</div></section></div>
