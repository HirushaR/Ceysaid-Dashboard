<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance / Invoices</p><div class="flex items-center gap-3"><h1>{{ $invoice->invoice_number }}</h1><span class="status-badge">{{ ucfirst($invoice->customer_payment_status) }}</span></div><p>{{ $invoice->lead?->customer_name }} · Due {{ $invoice->due_date?->format('d M Y') }}</p></div><div class="flex gap-2"><a target="_blank" href="{{ route('finance.invoices.pdf',$invoice) }}" class="btn-secondary">PDF</a><a href="{{ route('admin.leads.show',$invoice->lead) }}" class="btn-secondary">View lead</a></div></div><section class="grid gap-4 md:grid-cols-3">@foreach([['Invoice total',$invoice->total_amount],['Received',$invoice->payment_amount],['Outstanding',$invoice->balance_amount]] as [$label,$amount])<article class="metric-card"><p>{{ $label }}</p><strong>LKR {{ number_format((float)$amount,2) }}</strong></article>@endforeach</section><div class="grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><section class="panel"><div class="panel-header"><h2>Invoice lines</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Description</th><th>Qty</th><th>Rate</th><th>Amount</th></tr></thead><tbody>@foreach($invoice->lineItems as $line)<tr><td>{{ $line->description }}</td><td>{{ $line->quantity }}</td><td>LKR {{ number_format((float)$line->rate,2) }}</td><td>LKR {{ number_format((float)$line->amount,2) }}</td></tr>@endforeach</tbody></table></div></section><aside class="space-y-6"><section class="panel p-5"><h2 class="font-bold">Customer receipts</h2><div class="mt-4 space-y-3">@forelse($invoice->customerPayments as $payment)<div class="flex justify-between border-b border-slate-100 pb-3 dark:border-slate-800"><div><p class="text-sm font-semibold">{{ $payment->receipt_number }}</p><p class="text-xs text-slate-500">{{ $payment->payment_date?->format('d M Y') }}</p></div><strong>LKR {{ number_format((float)$payment->amount,2) }}</strong></div>@empty<p class="text-sm text-slate-500">No receipts recorded.</p>@endforelse</div></section><section class="panel p-5"><h2 class="font-bold">Supplier bills</h2><div class="mt-4 space-y-3">@forelse($invoice->vendorBills as $bill)<div class="flex justify-between text-sm"><span>{{ $bill->supplier?->name ?? $bill->vendor_name }}</span><span class="font-semibold">LKR {{ number_format((float)$bill->outstanding_amount,2) }}</span></div>@empty<p class="text-sm text-slate-500">No supplier bills.</p>@endforelse</div></section></aside></div></div>
