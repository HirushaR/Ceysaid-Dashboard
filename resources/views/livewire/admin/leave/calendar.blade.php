<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Employee services</p><h1>Leave calendar</h1><p>Approved leave and office closures.</p></div><input wire:model.live="month" type="month" class="form-input w-auto"></div><div class="grid gap-6 lg:grid-cols-2"><section class="panel"><div class="panel-header"><h2>Approved leave</h2></div><div class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($leaves as $leave)<div class="flex justify-between p-4"><div><p class="font-semibold">{{ $leave->user?->name }}</p><p class="text-sm text-slate-500">{{ $leave->type->getLabel() }}</p></div><p class="text-sm">{{ $leave->start_date->format('d M') }} – {{ $leave->end_date->format('d M') }}</p></div>@empty<div class="empty-state">No approved leave this month.</div>@endforelse</div></section><section class="panel"><div class="panel-header"><h2>Closures and holidays</h2></div><div class="divide-y divide-slate-100 dark:divide-slate-800">@forelse($closures as $closure)<div class="flex justify-between p-4"><div><p class="font-semibold">{{ $closure->title }}</p><p class="text-sm text-slate-500">{{ $closure->type->getLabel() }}</p></div><p class="text-sm">{{ $closure->start_date->format('d M') }} – {{ $closure->end_date->format('d M') }}</p></div>@empty<div class="empty-state">No closures this month.</div>@endforelse</div></section></div></div>
