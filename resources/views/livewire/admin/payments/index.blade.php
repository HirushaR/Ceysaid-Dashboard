<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance</p><h1>Payment register</h1><p>Incoming customer receipts and outgoing supplier payments.</p></div>@if(auth()->user()->canManageAccountingRecords())<a href="{{ route('admin.supplier-payments.create') }}" class="btn-primary">Record supplier payment</a>@endif</div><section class="grid gap-4 md:grid-cols-3">@foreach([['Received',$summary['received']],['Paid',$summary['paid']],['Net movement',$summary['net']]] as [$label,$amount])<article class="metric-card"><p>{{ $label }}</p><strong>LKR {{ number_format($amount,2) }}</strong></article>@endforeach</section><section class="panel"><div class="grid gap-3 border-b border-slate-100 p-4 sm:grid-cols-3 dark:border-slate-800"><select wire:model.live="direction" class="form-input"><option value="">All directions</option><option value="in">Received</option><option value="out">Paid</option></select><input wire:model.live="date_from" type="date" class="form-input"><input wire:model.live="date_to" type="date" class="form-input"></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Date</th><th>Direction</th><th>Reference</th><th>Party</th><th>Method</th><th>Amount</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ \Carbon\Carbon::parse($payment->payment_date)->format('d M Y') }}</td><td><span class="status-badge">{{ $payment->direction==='in'?'Received':'Paid' }}</span></td><td>{{ $payment->reference }}</td><td>{{ $payment->party }}</td><td>{{ str_replace('_',' ',ucfirst($payment->payment_method)) }}</td><td class="font-semibold {{ $payment->direction==='in'?'text-emerald-600':'text-rose-600' }}">{{ $payment->direction==='in'?'+':'−' }} LKR {{ number_format((float)$payment->amount,2) }}</td></tr>@empty<tr><td colspan="6" class="empty-state">No transactions match these filters.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $payments->links() }}</div></section></div>
