<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance</p><h1>Quotes</h1><p>Versioned customer proposals from draft through conversion.</p></div><a href="{{ route('admin.quotes.create',$lead?['lead'=>$lead]:[]) }}" class="btn-primary">+ New quote</a></div><section class="panel"><div class="border-b border-slate-100 p-4 dark:border-slate-800"><input wire:model.live.debounce.300ms="search" class="form-input max-w-lg" placeholder="Search quote, customer, or lead…"></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Quote</th><th>Customer</th><th>Revision</th><th>Status</th><th>Valid until</th><th>Total</th></tr></thead><tbody>@forelse($quotes as $quote)<tr><td><a class="table-link" href="{{ route('admin.quotes.show',$quote) }}">{{ $quote->quote_number }}</a></td><td>{{ $quote->lead?->customer_name }}</td><td>R{{ $quote->revision }}</td><td><span class="status-badge">{{ $quote->status->label() }}</span></td><td>{{ $quote->valid_until?->format('d M Y') ?? '—' }}</td><td>LKR {{ number_format($quote->totalAmount(),2) }}</td></tr>@empty<tr><td colspan="6" class="empty-state">No quotes found.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $quotes->links() }}</div></section></div>
