<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance</p><h1>Customer receivables</h1><p>Open and overdue customer invoice balances.</p></div></div><section class="grid gap-4 md:grid-cols-3"><article class="metric-card"><p>Outstanding</p><strong>LKR {{ number_format($summary['outstanding'],2) }}</strong></article><article class="metric-card"><p>Overdue</p><strong>LKR {{ number_format($summary['overdue'],2) }}</strong></article><article class="metric-card"><p>Open invoices</p><strong>{{ $summary['count'] }}</strong></article></section><section class="panel"><div class="grid gap-3 p-4 sm:grid-cols-2"><input wire:model.live.debounce.300ms="search" class="form-input" placeholder="Search invoice or customer"><select wire:model.live="status" class="form-input"><option value="">All open</option><option value="overdue">Overdue only</option></select></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Invoice</th><th>Customer</th><th>Due</th><th>Status</th><th>Outstanding</th><th></th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td><a href="{{ route('admin.invoices.show',$invoice) }}" class="font-semibold text-blue-600">{{ $invoice->invoice_number }}</a></td><td>{{ $invoice->lead?->customer_name }}</td><td>{{ $invoice->due_date?->format('d M Y') }}</td><td>@if($invoice->due_date?->isPast())<span class="status-badge text-rose-600">Overdue</span>@else<span class="status-badge">Open</span>@endif</td><td class="font-semibold">LKR {{ number_format((float)$invoice->balance_amount,2) }}</td><td><a href="{{ route('admin.invoices.payments.create',$invoice) }}" class="text-sm font-semibold text-blue-600">Record receipt</a></td></tr>@empty<tr><td colspan="6" class="empty-state">No open receivables.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $invoices->links() }}</div></section></div>
