<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance</p><h1>Suppliers</h1><p>Supplier master records and outstanding payables.</p></div>@if(auth()->user()->canManageAccountingRecords())<a href="{{ route('admin.suppliers.create') }}" class="btn-primary">New supplier</a>@endif</div><section class="panel"><div class="p-4"><input wire:model.live.debounce.300ms="search" class="form-input max-w-lg" placeholder="Search suppliers…"></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Supplier</th><th>Contact</th><th>Bills</th><th>Outstanding</th></tr></thead><tbody>@forelse($suppliers as $supplier)<tr><td><a href="{{ route('admin.suppliers.show',$supplier) }}" class="font-semibold text-blue-600">{{ $supplier->name }}</a></td><td>{{ $supplier->contact_name ?: '—' }}<p class="text-xs text-slate-500">{{ $supplier->email }}</p></td><td>{{ $supplier->vendor_bills_count }}</td><td class="font-semibold">LKR {{ number_format($supplier->totalOutstandingPayable(),2) }}</td></tr>@empty<tr><td colspan="4" class="empty-state">No suppliers found.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $suppliers->links() }}</div></section></div>
