<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance / Suppliers</p><h1>{{ $supplier->name }}</h1><p>{{ $supplier->contact_name ?: 'No contact person' }} · {{ $supplier->email ?: 'No email' }}</p></div><div class="flex gap-2">@if(auth()->user()->canManageAccountingRecords())<a href="{{ route('admin.supplier-payments.create',['supplier'=>$supplier->id]) }}" class="btn-primary">Record payment</a>@endif<a target="_blank" href="{{ route('finance.supplier-payables.pdf',$supplier) }}" class="btn-secondary">Payables PDF</a></div></div><section class="grid gap-4 md:grid-cols-3"><article class="metric-card"><p>Outstanding</p><strong>LKR {{ number_format($supplier->totalOutstandingPayable(),2) }}</strong></article><article class="metric-card"><p>Total bills</p><strong>{{ $supplier->vendorBills->count() }}</strong></article><article class="metric-card"><p>Payments</p><strong>{{ $supplier->supplierPayments->count() }}</strong></article></section><div class="grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><section class="panel"><div class="panel-header"><h2>Vendor bills</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Bill</th><th>Customer</th><th>Due</th><th>Status</th><th>Outstanding</th></tr></thead><tbody>@forelse($supplier->vendorBills as $bill)<tr><td><a href="{{ route('admin.vendor-bills.edit',$bill) }}" class="font-semibold text-blue-600">{{ $bill->vendor_bill_number }}</a></td><td>{{ $bill->invoice?->lead?->customer_name }}</td><td>{{ $bill->due_date?->format('d M Y') }}</td><td><span class="status-badge">{{ ucfirst($bill->payment_status) }}</span></td><td>LKR {{ number_format($bill->outstanding_amount,2) }}</td></tr>@empty<tr><td colspan="5" class="empty-state">No bills for this supplier.</td></tr>@endforelse</tbody></table></div></section><aside class="panel p-5"><h2 class="font-bold">Payment history</h2><div class="mt-4 space-y-4">@forelse($supplier->supplierPayments->sortByDesc('payment_date') as $payment)<div class="border-b border-slate-100 pb-3 dark:border-slate-800"><div class="flex justify-between"><span class="font-semibold">{{ $payment->payment_number }}</span><strong>LKR {{ number_format((float)$payment->amount,2) }}</strong></div><p class="text-xs text-slate-500">{{ $payment->payment_date?->format('d M Y') }} · {{ str_replace('_',' ',$payment->payment_mode) }}</p></div>@empty<p class="text-sm text-slate-500">No supplier payments.</p>@endforelse</div></aside></div></div>
