<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Operations</p><h1>Tours</h1><p>Group departures, capacity and financial performance.</p></div><a href="{{ route('admin.tours.create') }}" class="btn-primary">New tour</a></div><section class="panel"><div class="grid gap-3 p-4 sm:grid-cols-2"><input wire:model.live.debounce.300ms="search" class="form-input" placeholder="Search tour"><select wire:model.live="status" class="form-input"><option value="">All statuses</option>@foreach($statuses as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Tour</th><th>Departure</th><th>Status</th><th>Booked</th><th>Capacity</th><th>Price</th></tr></thead><tbody>@forelse($tours as $tour)<tr><td><a href="{{ route('admin.tours.show',$tour) }}" class="font-semibold text-blue-600">{{ $tour->tour_code }}</a><p class="text-xs text-slate-500">{{ $tour->name }}</p></td><td>{{ $tour->departure_date?->format('d M Y') }}</td><td><span class="status-badge">{{ $tour->status->label() }}</span></td><td>{{ $tour->booked_seats }}</td><td>{{ $tour->seat_capacity ?: '—' }}</td><td>{{ $tour->currency }} {{ number_format((float)$tour->package_price,2) }}</td></tr>@empty<tr><td colspan="6" class="empty-state">No tours found.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $tours->links() }}</div></section></div>
