<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Operations / Tours</p><div class="flex items-center gap-3"><h1>{{ $tour->tour_code }}</h1><span class="status-badge">{{ $tour->status->label() }}</span></div><p>{{ $tour->name }} · {{ $tour->departure_date?->format('d M Y') }}</p></div><a href="{{ route('admin.tours.edit',$tour) }}" class="btn-primary">Edit tour</a></div><section class="grid gap-4 md:grid-cols-4">@foreach([['Booked / capacity',$tour->booked_seats.' / '.($tour->seat_capacity?:'—')],['Revenue','LKR '.number_format($summary['revenue'],2)],['Supplier costs','LKR '.number_format($summary['supplierCosts'],2)],['Gross profit','LKR '.number_format($summary['profit'],2)]] as [$label,$value])<article class="metric-card"><p>{{ $label }}</p><strong>{{ $value }}</strong></article>@endforeach</section><section class="grid gap-4 md:grid-cols-4">@foreach([['Received',$summary['received']],['Receivable',$summary['receivable']],['Supplier paid',$summary['supplierPaid']],['Payable',$summary['payable']]] as [$label,$value])<article class="metric-card"><p>{{ $label }}</p><strong>LKR {{ number_format($value,2) }}</strong></article>@endforeach</section><section class="panel"><div class="panel-header"><h2>Bookings</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Lead</th><th>Customer</th><th>Pax</th><th>Sales</th><th>Status</th></tr></thead><tbody>@forelse($tour->leads as $lead)<tr><td><a href="{{ route('admin.leads.show',$lead) }}" class="font-semibold text-blue-600">{{ $lead->reference_id ?: '#'.$lead->id }}</a></td><td>{{ $lead->customer_name }}</td><td>{{ $lead->booked_pax }}</td><td>{{ $lead->assignedUser?->name ?: '—' }}</td><td><span class="status-badge">{{ \App\Enums\LeadStatus::tryFrom((string)$lead->status)?->label() ?? $lead->status }}</span></td></tr>@empty<tr><td colspan="5" class="empty-state">No group bookings linked.</td></tr>@endforelse</tbody></table></div></section></div>
