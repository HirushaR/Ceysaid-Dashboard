<div class="mx-auto max-w-5xl space-y-6"><div class="page-heading"><div><p class="eyebrow">Administration / Users</p><h1>{{ $heading }}</h1><p>Role controls the primary workflow; groups and overrides add specific capabilities.</p></div><a href="{{ route('admin.users.index') }}" class="btn-secondary">Back</a></div><form wire:submit="save" class="space-y-6"><section class="panel p-6"><div class="grid gap-5 md:grid-cols-2"><div><label class="form-label">Name *</label><input wire:model="name" class="form-input"></div><div><label class="form-label">Email *</label><input wire:model="email" type="email" class="form-input"></div><div><label class="form-label">Role *</label><select wire:model.live="role" class="form-input">@foreach($roles as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></div><div class="flex items-center gap-3 pt-7"><input wire:model="is_manager" type="checkbox"><label>Manager of the same-role team</label></div><div><label class="form-label">{{ $editing?'New password':'Password *' }}</label><input wire:model="password" type="password" class="form-input" placeholder="{{ $editing?'Leave blank to keep current':'' }}"></div></div></section>@if(auth()->user()->isAdmin())<section class="panel p-6"><h2 class="font-bold">Permission groups</h2><div class="mt-4 grid gap-3 md:grid-cols-2">@foreach($groups as $group)<label class="flex gap-3 rounded-xl border border-slate-200 p-3 dark:border-slate-700"><input wire:model="group_ids" type="checkbox" value="{{ $group->id }}"><span><strong>{{ $group->display_name }}</strong><small class="block text-slate-500">{{ $group->description }}</small></span></label>@endforeach</div></section><section class="panel p-6"><h2 class="font-bold">Individual permission overrides</h2><p class="text-sm text-slate-500">Use these sparingly; reusable access belongs in a group.</p><div class="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">@foreach($permissions as $permission)<label class="flex gap-2 text-sm"><input wire:model="permission_ids" type="checkbox" value="{{ $permission->id }}"><span>{{ $permission->display_name }}</span></label>@endforeach</div></section>@endif<div class="flex justify-end"><button class="btn-primary">{{ $submitLabel }}</button></div></form></div>
