<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Administration</p><h1>Users and teams</h1><p>Roles, managers and effective access.</p></div><a href="{{ route('admin.users.create') }}" class="btn-primary">New user</a></div><section class="panel"><div class="grid gap-3 p-4 sm:grid-cols-2"><input wire:model.live.debounce.300ms="search" class="form-input" placeholder="Search name or email"><select wire:model.live="role" class="form-input"><option value="">All roles</option>@foreach($roles as $value=>$label)<option value="{{ $value }}">{{ $label }}</option>@endforeach</select></div><div class="table-wrap"><table class="data-table"><thead><tr><th>User</th><th>Role</th><th>Manager</th><th>Groups</th><th>Direct permissions</th><th></th></tr></thead><tbody>@forelse($users as $user)<tr><td><p class="font-semibold">{{ $user->name }}</p><p class="text-xs text-slate-500">{{ $user->email }}</p></td><td><span class="status-badge">{{ $roles[$user->role]??ucfirst($user->role) }}</span></td><td>{{ $user->is_manager?'Yes':'No' }}</td><td>{{ $user->permission_groups_count }}</td><td>{{ $user->permissions_count }}</td><td>@if(auth()->id()!==$user->id && (auth()->user()->isAdmin()||!$user->isAdmin()))<a href="{{ route('admin.users.edit',$user) }}" class="text-sm font-semibold text-blue-600">Manage</a>@else<span class="text-xs text-slate-400">Current user</span>@endif</td></tr>@empty<tr><td colspan="6" class="empty-state">No users found.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $users->links() }}</div></section></div>
