<div class="space-y-6"><div class="page-heading"><div><p class="eyebrow">Finance / Vendor bills</p><div class="flex items-center gap-3"><h1>{{ $vendorBill->vendor_bill_number }}</h1><span class="status-badge">{{ ucfirst($vendorBill->payment_status) }}</span></div><p>{{ $vendorBill->supplier?->name ?? $vendorBill->vendor_name }} · {{ $vendorBill->invoice?->lead?->customer_name }}</p></div><div class="flex gap-2">@if(auth()->user()->canManageAccountingRecords())<a href="{{ route('admin.vendor-bills.edit',$vendorBill) }}" class="btn-primary">Edit bill</a>@endif<a target="_blank" href="{{ route('finance.vendor-bills.pdf',$vendorBill) }}" class="btn-secondary">PDF</a></div></div><section class="grid gap-4 md:grid-cols-3"><article class="metric-card"><p>Bill total</p><strong>LKR {{ number_format((float)$vendorBill->bill_amount,2) }}</strong></article><article class="metric-card"><p>Paid</p><strong>LKR {{ number_format($vendorBill->total_paid_amount,2) }}</strong></article><article class="metric-card"><p>Outstanding</p><strong>LKR {{ number_format($vendorBill->outstanding_amount,2) }}</strong></article></section><div class="grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><section class="panel"><div class="panel-header"><h2>Bill lines</h2></div><div class="table-wrap"><table class="data-table"><thead><tr><th>Description</th><th>Qty</th><th>Rate</th><th>Amount</th></tr></thead><tbody>@foreach($vendorBill->lineItems as $line)<tr><td>{{ $line->description }}</td><td>{{ $line->quantity }}</td><td>LKR {{ number_format((float)$line->rate,2) }}</td><td>LKR {{ number_format((float)$line->amount,2) }}</td></tr>@endforeach</tbody></table></div></section><aside class="panel p-5"><h2 class="font-bold">Payment timeline</h2><div class="mt-4 space-y-4">@forelse($vendorBill->vendorBillPayments as $payment)<div class="border-l-2 border-blue-500 pl-4"><div class="flex justify-between"><span class="font-semibold">{{ $payment->supplierPayment?->payment_number ?? 'Legacy payment' }}</span><strong>LKR {{ number_format((float)$payment->amount,2) }}</strong></div><p class="text-xs text-slate-500">{{ $payment->payment_date?->format('d M Y') }} · {{ str_replace('_',' ',$payment->payment_mode) }}</p>@if($payment->supplierPayment)<a href="{{ route('admin.payments.supplier.show',$payment->supplierPayment) }}" class="text-xs font-semibold text-blue-600">View payment</a>@endif</div>@empty<p class="text-sm text-slate-500">No payments recorded.</p>@endforelse</div></aside></div></div>
